// Определение функции sum
function sum(m, n) {
  return m + n;
}

// Проверка условий
var result1 = sum(1, 1);
var result2 = sum(2, 5);

// Вывод результатов на экран
alert("Результат sum(1, 1) === 2: " + (result1 === 2));
alert("Результат sum(2, 5) === 7: " + (result2 === 7));



